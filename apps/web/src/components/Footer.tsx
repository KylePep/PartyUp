export function Footer() {
  return (
    <footer className="border-t border-brand-border py-6 px-8 text-center shrink-0">
      <p className="font-mono text-brand-muted/50 text-xs tracking-widest uppercase">
        PartyUp &copy; {new Date().getFullYear()} — Find Your Player Two
      </p>
    </footer>
  );
}
